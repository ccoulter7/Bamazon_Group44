//Function to change the CSS styleSheet


//Take twos paramaters, link and the name of stylesheet

function changeTheme(sheet)  {
    document.getElementById('pagestyleTheme').setAttribute('href',sheet);
}